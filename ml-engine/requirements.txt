fastapi>=0.115.0
uvicorn[standard]>=0.52.1
pydantic>=2.8.0
python-multipart>=0.0.9
pandas>=3.0.5
numpy>=1.26.0
scikit-learn>=1.9.0
# Pinned to the versions used to train models/predictor.joblib. XGBoost/LightGBM
# pickles are not readable across major versions, so the trainer and the runtime
# must agree on these. Retrain after bumping: python -m train.train_predictor
xgboost==3.3.0
lightgbm==4.7.0
joblib>=1.5.3
requests>=2.32.0
Pillow>=10.3.0
pytest>=9.1.1

# Optional: plant recognition with deep learning
# tensorflow>=2.16.0
# torch>=2.3.0
# torchvision>=0.18.0
# transformers>=4.40.0
# langchain>=0.2.0
# qdrant-client>=1.9.0
# openai>=1.30.0
